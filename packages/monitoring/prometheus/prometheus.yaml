scrape_configs:
  - job_name: novelty
    static_configs:
      - targets: ["localhost:3001"]
    scrape_interval: 10s

  - job_name: node
    static_configs:
      - targets: ["localhost:9100"]

rule_files:
  - /etc/prometheus/alert.rules.yml
